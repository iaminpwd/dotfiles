{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests", ":configMigration", "abandonments:recommended"],
  "timezone": "Asia/Seoul",
  "schedule": ["before 6am on monday"],
  "osvVulnerabilityAlerts": true,
  "packageRules": [
    {
      "matchManagers": ["mise"],
      "groupName": "mise 도구 버전 업데이트",
      "labels": ["dependencies", "mise"]
    },
    {
      "matchManagers": ["mise"],
      "matchDepNames": [
        "checkov",
        "fzf",
        "grype",
        "hadolint",
        "infracost",
        "k3d",
        "k9s",
        "kubectx",
        "pluto",
        "promtool",
        "syft",
        "tflint"
      ],
      "overrideDatasource": "github-releases",
      "description": [
        "아래 도구들은 mise가 aqua 백엔드로 받고, 그 aqua 패키지가 type: github_release 라 GitHub Release 에셋이 반드시 있어야 설치된다. 그런데 Renovate 기본 datasource가 github-tags여서, 업스트림이 태그만 밀고 릴리스를 아직 발행하지 않은 버전까지 업데이트로 제안한다. 그러면 mise install이 404로 죽어 CI 전 job이 실패한다.",
        "실측(checkov): 3.3.11은 refs/tags에도 PyPI에도 있지만 GitHub Release가 없어(최신 릴리스 3.3.9) PR #5의 3개 job이 전부 실패했다. 조회 대상을 릴리스로 맞추자 해당 범프가 제안 목록에서 사라지며 CI가 통과했다.",
        "목록 선정 기준은 추측이 아니라 두 사실의 교집합이다. (1) aquaproj/aqua-registry의 해당 pkg가 type: github_release 인가, (2) Renovate가 실제로 github-tags로 조회 중인가(과거 PR 본문의 age 배지 URL로 확인). 둘 다 참인 도구만 넣었고, 전부 최신 릴리스가 실재함을 확인해 이 오버라이드가 업데이트를 동결시키지 않는 것도 검증했다.",
        "의도적 제외 — awscli/terraform/kubectl/helm은 aqua type: http 라 각자 벤더 CDN에서 받고 GitHub Release를 안 쓴다(특히 aws/aws-cli는 최신 릴리스가 2.0.0dev0 이라 오버라이드하면 오히려 깨진다). go/node/python은 aqua가 아니라 core: 백엔드고 golang/go는 릴리스가 아예 없다. ansible 등 pipx: 계열은 PyPI datasource가 정답이다. loki-logcli는 packageName이 grafana/loki/logcli 3단 경로라 datasource만 바꾸면 조회가 깨져 조용히 동결될 위험이 있어 제외했다.",
        "새 도구를 config.toml에 추가할 때: `mise registry <도구>`로 백엔드를 보고, aqua면 aqua-registry의 type을 확인해 github_release인 경우 이 목록에 함께 넣을 것."
      ]
    },
    {
      "matchManagers": ["custom.regex"],
      "groupName": "zsh 플러그인 버전 업데이트",
      "labels": ["dependencies", "zsh"]
    },
    {
      "matchDepNames": ["oh-my-zsh"],
      "minimumReleaseAge": "14 days",
      "description": "oh-my-zsh는 릴리스 태그 없이 master 커밋을 그대로 추적하므로, 태그 기반 의존성과 달리 메인테이너의 사전 검증 절차가 없음. 갓 올라온 커밋이 되돌려지거나 문제로 드러날 시간을 벌기 위해 14일 숙성 기간을 둠."
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "ansible.builtin.git으로 커밋 SHA 고정된 oh-my-zsh 본체를 master 브랜치 최신 커밋으로 추적",
      "managerFilePatterns": ["ansible/roles/zsh/tasks/main.yml"],
      "matchStrings": [
        "repo: '(?<packageName>https://github\\.com/ohmyzsh/ohmyzsh)\\.git'[\\s\\S]*?version: (?<currentDigest>[0-9a-f]{40})"
      ],
      "depNameTemplate": "oh-my-zsh",
      "currentValueTemplate": "master",
      "datasourceTemplate": "git-refs"
    },
    {
      "customType": "regex",
      "description": "zsh-autosuggestions 플러그인은 릴리스 태그가 존재하므로 master 커밋 대신 최신 안정 태그를 추적",
      "managerFilePatterns": ["ansible/roles/zsh/tasks/main.yml"],
      "matchStrings": [
        "repo: 'https://github\\.com/(?<packageName>zsh-users/zsh-autosuggestions)\\.git'[\\s\\S]*?version: (?<currentValue>\\S+)"
      ],
      "depNameTemplate": "zsh-autosuggestions",
      "datasourceTemplate": "github-tags"
    },
    {
      "customType": "regex",
      "description": "zsh-syntax-highlighting 플러그인은 릴리스 태그가 존재하므로 master 커밋 대신 최신 안정 태그를 추적",
      "managerFilePatterns": ["ansible/roles/zsh/tasks/main.yml"],
      "matchStrings": [
        "repo: 'https://github\\.com/(?<packageName>zsh-users/zsh-syntax-highlighting)\\.git'[\\s\\S]*?version: (?<currentValue>\\S+)"
      ],
      "depNameTemplate": "zsh-syntax-highlighting",
      "datasourceTemplate": "github-tags"
    }
  ]
}
