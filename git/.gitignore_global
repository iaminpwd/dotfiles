# ==============================================================================
# Global Git Ignore File (System-wide)
# Prevents catastrophic leaks of IaC states, secrets, and environment configurations
# ==============================================================================

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
crash.log
*.tfvars

# Secrets & Credentials
*.pem
*.key
*.cert
*.crt
.env
.env.local
.env.*
secrets.yml
*.ovpn
kubeconfig
.kube/config

# Cloud Providers
.aws/
.azure/
.gcp/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs / Editors
.idea/
.vscode/
*.swp
*.swo
*~

# AI Context Files (Prevent AI rule pollution in remote repos)
.agents/
.aiexclude
AGENTS.md
CLAUDE.md

# Local validation helper scripts (각 프로젝트 루트에 생성되는 심볼릭 링크만 제외 - 원본 스크립트는 슬래시 없는 패턴이면 저장소 전역에서 무시되어 버림)
/pre-flight-check.sh

# Pre-flight check cache and timestamp files
.pre-flight-check.cache
.trivy-db-update.timestamp

**/.claude/settings.local.json
