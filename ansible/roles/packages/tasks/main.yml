---
- name: OS 패키지 설치 (Debian 계열)
  ansible.builtin.apt:
    name:
      - git
      - curl
      - unzip
      - wget
      - zsh
      - stow
      - tree
      - ca-certificates
      - gnupg
      - dnsutils
      - gawk
    state: present
    update_cache: true
    cache_valid_time: 3600 # 1시간 이내 재실행 시 apt update 스킵
  become: true
  when: ansible_os_family == "Debian"

- name: EPEL 저장소 활성화 (RHEL/Rocky/CentOS)
  ansible.builtin.dnf:
    name: epel-release
    state: present
  become: true
  when:
    - ansible_os_family == "RedHat"
    - ansible_distribution != "Fedora"

- name: OS 패키지 설치 (RedHat 계열)
  ansible.builtin.dnf:
    name:
      - git
      - curl
      - unzip
      - wget
      - zsh
      - stow
      - tree
      - ca-certificates
      - gnupg2
      - bind-utils
      - gawk
    state: present
  become: true
  when: ansible_os_family == "RedHat"

- name: OS 패키지 설치 (macOS Homebrew)
  community.general.homebrew:
    name:
      - git
      - curl
      - unzip
      - wget
      - zsh
      - stow
      - tree
      - bash
      - coreutils
      - gnu-sed
      - findutils
      - grep
    state: present
  when: ansible_os_family == "Darwin"
